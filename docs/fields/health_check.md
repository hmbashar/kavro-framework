# `health_check` Field

## Purpose

The `health_check` field is available in Kavro option panels and metaboxes.

## Example

```php
array(
    'id'    => 'example_health_check',
    'type'  => 'health_check',
    'title' => 'Health Check',
)
```

## Demo

This field is included in:

- `examples/options-demo.php`
- `examples/metabox-demo.php`
- `examples/field-examples.php`
